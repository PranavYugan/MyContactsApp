This use case lets a logged-in user assign one or multiple tags to contacts.  
It applies concepts like adding or removing tags and maintaining object relationships between Contact and Tag.  
Java concepts include using add() and remove() methods in collections with basic loop handling for tag management.  
