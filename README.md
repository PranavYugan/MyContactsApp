This use case enables a logged-in user to apply filters such as tag, date added, or frequently contacted.  
It uses OOP concepts like a simple filter interface with separate methods for each filter type.  
Java concepts include Comparator for sorting, Collections.sort(), and loops with conditional checks.  
